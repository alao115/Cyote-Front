const state = () => ({})

const getters = {}

const mutations = {}

const actions = {
  /* nuxtServerInit({ dispatch, getters, commit }, { route }) {
    // eslint-disable-next-line no-async-promise-executor
    return new Promise(async (resolve, reject) => {
      try {
        await dispatch('patrol/fetchPatrols')
        resolve()
      } catch (err) {
        reject(err)
      }
    })
  }, */
}

export default {
  state,
  getters,
  mutations,
  actions,
}
